{
  "private": true,
  "scripts": {
    "dev": "nodify dev",
    "build": "nodify build",
    "start": "npm run build && node ./build/main"
  },
  "devDependencies": {
    "bs-platform": "^2.2.2",
    "nodify-core": "^5.0.0",
    "rollup-plugin-bucklescript": "^0.6.1"
  }
}
