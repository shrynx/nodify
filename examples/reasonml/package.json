{
  "private": true,
  "scripts": {
    "dev": "nodify dev",
    "build": "nodify build",
    "start": "npm run build && node ./build/main"
  },
  "devDependencies": {
    "bs-platform": "^2.0.0",
    "nodify-core": "^2.0.2",
    "rollup-plugin-bucklescript": "^0.6.1"
  }
}
