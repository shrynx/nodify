{
  "private": true,
  "scripts": {
    "dev": "nodify dev",
    "build": "nodify build",
    "start": "npm run build && node ./build/main"
  },
  "devDependencies": {
    "nodify-core": "^4.0.5",
    "rollup-plugin-typescript": "^0.8.1"
  }
}
